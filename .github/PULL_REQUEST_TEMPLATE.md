## What this changes

<!-- One or two sentences: what does this PR do, and why. -->

## Related issue

Closes #

## Checklist

- [ ] `npm run build` passes locally
- [ ] `npm run lint` passes locally
- [ ] `npm test` passes locally
- [ ] UI copy is in English
- [ ] No new dependency was added, or if one was, the description above explains why it's necessary
- [ ] `import "server-only"` is present on any new module that fetches data or reads env vars

## Screenshots / recording

<!-- Required if this touches UI. Before/after screenshots for a static change; a short recording for anything
interactive (drag, hover, animation, export preview). Delete this section if the PR has no UI-visible change. -->

## Notes for the reviewer

<!-- Anything that isn't obvious from the diff: a tradeoff you made, something you're unsure about, an area you'd
like specific feedback on. Delete if not needed. -->
